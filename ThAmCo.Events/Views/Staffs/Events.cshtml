@model StaffEventsVM
@{
    ViewData["Title"] = "Events";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@Model.staff.Forename @Model.staff.Surname Events</h1>

<p>
    <a asp-action="AddEvent" asp-route-id=@Model.staff.StaffId>Add an Event</a>

</p>
@{
    if (Model.events.Count() > 0)
    {

        <table class="table">
            <thead>
                <tr>
                    <th>
                        Name
                    </th>
                    <th>
                        Date and Time
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.events)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.EventTitle)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EventDate)
                        </td>
                        <td>
                            @Html.ActionLink("Remove","Remove","Staffs", new { eventID = item.EventId, staffId = @Model.staff.StaffId})
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    }
    else
    {
        <p>
            This staff member has no events
        </p>
    }
}


